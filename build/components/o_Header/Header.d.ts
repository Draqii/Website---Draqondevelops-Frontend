import React from "react";
import { HeaderProps } from "./Header.types";
import "./Header.scss";
declare const Header: ({ language, theme, className }: HeaderProps) => React.JSX.Element;
export default Header;
